次のサーバ設定が変更されます：

%@（%@）は WAN 接続として使用されます。その接続は、次のポートで共有されます：
%@
DHCP が入になり、次のサブネットを使用して設定されます：
%@
これまで DHCP に設定されたサブネットはすべて取り除かれます。

IP ファイアウォールが入になり、次の IP アドレスグループを使用して設定されます：
%@ 
NAT が入になり、%@（%@）のネットワークが共有されます

VPN が入になります（%@）

DNS が入になります